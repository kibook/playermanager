Config = {}

-- Which type of player identifier to use in the database (license, steam, discord, ip, etc.)
Config.dbIdentifier = "license"

Config.queue = {}

-- Whether to enable the queue or not
Config.queue.enabled = true

-- The maximum size of the queue. Set to false for unlimited size.
Config.queue.size = false
